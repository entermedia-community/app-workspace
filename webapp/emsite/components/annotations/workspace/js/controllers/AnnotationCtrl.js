// Generated by CoffeeScript 1.4.0

Workspace.controller('AnnotationCtrl', [
  '$rootScope', '$scope', '$filter', 'ngTableParams', 'collectionAssetData', function($rootScope, $scope, $filter, ngTableParams, collectionAssetData) {
    var data;
    $rootScope.$broadcast('navigatedTo', 'Annotations');
    data = collectionAssetData.assetData;
    $scope.tableParams = new ngTableParams({
      page: 1,
      count: 5,
      sorting: {
        'annotation.hasRecentActivity': 'desc'
      }
    }, {
      total: data.length,
      getData: function($defer, params) {
        var orderedData;
        orderedData = params.sorting() ? $filter('orderBy')(data, params.orderBy()) : data;
        $defer.resolve(orderedData.slice((params.page() - 1) * params.count(), params.page() * params.count()));
        return em.unit;
      }
    });
    return em.unit;
  }
]);
